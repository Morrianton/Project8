table
    tr
        th='UserID'
        th='Name'
        th='Age'
        th='Email Address'

each user in users
    tr
        td=user.userID
        td=user.name
        td=user.age
        td=user.email
        a(href='/users/' + user.id) Edit

